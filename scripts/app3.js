let numero1 = Number(prompt("Introduce un número"));
let numero2 = Number(prompt("Introduce otro número"));

for (let i = numero1; i <= numero2; i++) {

    console.log(`del ${numero1} hasta el ${numero2}, está el: ${i}`);
}